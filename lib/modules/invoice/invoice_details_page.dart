import 'package:flutter/material.dart';

class InvoiceDetailsPage extends StatelessWidget
{
  Widget build(BuildContext context)
  {
    return Scaffold(body: Center(child: Text('detailed invoice'),),);
  }
}